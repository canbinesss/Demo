<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.FofcsMapper">

    <select id="getlist" resultType="com.example.entity.Fofcs">
        select * from t_fofcs ORDER BY RAND() limit 0,10
    </select>
</mapper>